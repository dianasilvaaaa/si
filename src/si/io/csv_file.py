def read_csv(filename: str, sep: str = "", features, label):

"""   
filename – name/path of the file
sep – value separator
features – boolean. Does the file have feature names?
label – boolean. Does the file have y? (If yes, assume it's the last column)

"""   
